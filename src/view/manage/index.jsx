import React, { Component } from 'react';

class manage extends Component {
  render() {
    return (
      <div>
          {this.props.children}
      </div>
    );
  }
}

export default manage;
